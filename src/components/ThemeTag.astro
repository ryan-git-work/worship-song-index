---
interface Props {
  theme: string;
}

const { theme } = Astro.props;
---

<span class="inline-flex items-center px-3 py-1 rounded-md text-sm font-medium bg-primary-100 text-primary-800 border border-primary-200">
  {theme}
</span>
